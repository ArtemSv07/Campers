export const selectItems = (state) => state.campers.items;
export const selectItem = (state) => state.campers.selectItem;
export const selectTotalItems = (state) => state.campers.totalItems;
export const selectTotalItemsError = (state) => state.campers.error;
export const selectloading = (state) => state.campers.loading;
